const AMAZON = "/amazon";
const CAROUSEL_SLIDER = "/carousel-slider";
const SEARCH_AUTOCOMPLETE = "/autocomplete";

const routes = {
  amazon: AMAZON,
  carouselSlider: CAROUSEL_SLIDER,
  searchAutocomplete: SEARCH_AUTOCOMPLETE
};

export default routes;
