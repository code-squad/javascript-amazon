import NavigationView from "./NavigationView.js";

export default class Navigation {
  constructor({ navigationElement, options, carousel }) {
    new NavigationView({ navigationElement, options, carousel });
  }
}
