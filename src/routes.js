const AMAZON = "/amazon";
const CAROUSEL_SLIDER = "/carousel-slider";
const SEARCH = "/search";

const routes = {
  amazon: AMAZON,
  carouselSlider: CAROUSEL_SLIDER,
  search: SEARCH
};

export default routes;
